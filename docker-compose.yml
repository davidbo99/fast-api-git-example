version: "3"

services:
  app:
    build: 
      context: .
      args:
        DEV: "true"
//SE REALIZA EL PRIMER CAMBIO (ESTEBAN MAYA)
    env_file:
      - .env
    command: uvicorn main:app --reload --host 0.0.0.0 --port 8080
    volumes:
      - ./app:/app/
      - ./ml/model/:/app/ml/model/